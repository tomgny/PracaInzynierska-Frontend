import { LineRDirective } from './line-r.directive';

describe('LineRDirective', () => {
  it('should create an instance', () => {
    const directive = new LineRDirective();
    expect(directive).toBeTruthy();
  });
});
