import { LineRtDirective } from './line-rt.directive';

describe('LineRtDirective', () => {
  it('should create an instance', () => {
    const directive = new LineRtDirective();
    expect(directive).toBeTruthy();
  });
});
