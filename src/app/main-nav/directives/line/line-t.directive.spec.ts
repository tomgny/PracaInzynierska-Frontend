import { LineTDirective } from './line-t.directive';

describe('LineTDirective', () => {
  it('should create an instance', () => {
    const directive = new LineTDirective();
    expect(directive).toBeTruthy();
  });
});
