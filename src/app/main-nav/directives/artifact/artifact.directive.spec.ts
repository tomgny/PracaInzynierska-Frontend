import { ArtifactDirective } from './artifact.directive';

describe('ArtifactDirective', () => {
  it('should create an instance', () => {
    const directive = new ArtifactDirective();
    expect(directive).toBeTruthy();
  });
});
